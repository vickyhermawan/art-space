import React from "react";
import Hero from "@components/templates/home/index";

const Home: React.FC = ({ children }) => {
    return (
        <>
           <Hero/>
        </>
    );
};

export default Home;
